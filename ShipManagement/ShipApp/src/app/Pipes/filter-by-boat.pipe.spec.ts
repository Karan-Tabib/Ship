import { FilterByBoatPipe } from './filter-by-boat.pipe';

describe('FilterByBoatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBoatPipe();
    expect(pipe).toBeTruthy();
  });
});
