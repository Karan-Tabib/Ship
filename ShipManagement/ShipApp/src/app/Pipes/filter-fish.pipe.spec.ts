import { FilterFishPipe } from './filter-fish.pipe';

describe('FilterFishPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFishPipe();
    expect(pipe).toBeTruthy();
  });
});
